import Layout from "@/components/Layout";
import Main from "./Main";

const AboutPage = () => {
    return (
        <Layout emptyFooter>
            <Main />
        </Layout>
    );
};

export default AboutPage;
